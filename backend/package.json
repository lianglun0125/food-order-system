{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.2.2",
    "base64-js": "^1.5.1",
    "hono": "^4.11.1",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
